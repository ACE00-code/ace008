---------------  T H R E A D  ---------------

Current thread is native thread

siginfo: si_signo: 11 (SIGSEGV), si_code: 1 (SEGV_MAPERR), si_addr: 0x0000000000000000


当前线程是原生线程（native thread），可能是 JVM 内部线程（如 GC 线程）或 JNI 调用的本地代码。

VM 状态：not at safepoint（JVM 未处于安全点，说明崩溃发生在非 GC 阶段）。

Java 8u101 是一个较旧的版本，可能存在 已知的 JVM 崩溃 Bug（如某些 GC 或 JIT 编译问题）

PSYoungGen（新生代）使用了 6.4GB（78%），ParOldGen（老年代）使用了 153MB（0.7%）。

总堆大小：~31GB（PSYoungGen + ParOldGen）。

Metaspace（类元数据）使用了 72MB，未达到上限。

CodeCache（JIT 编译代码缓存）使用了 38MB，未满。

没有明显的 OOM（OutOfMemoryError），但可能存在 内存碎片或本地内存耗尽。

===========================================================

首页 50万次

企业信息基础信息 40万次
企业主搜索 20万次
企业预搜索 30万次

外部 70万次

推送 100万次

浏览记录40万次

===========================================================

Kubernetes 数据卷竞态读写问题及解决方案

===========================================================






